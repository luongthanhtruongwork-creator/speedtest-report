# BeyondNet VN - Speedtest Report
# Install: pip install -r requirements.txt
#
# NOTE: speedtest-cli (PyPI) KHÔNG được dùng — bị Ookla chặn 403
# Dùng Ookla Official CLI binary thay thế
# Xem hướng dẫn cài trong CLAUDE.md

requests>=2.28.0
reportlab>=3.6.0
matplotlib>=3.5.0
